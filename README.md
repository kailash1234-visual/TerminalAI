# AI Terminal Chat
A simple terminal chatbot powered by Groq AI.

## Setup
1. Install Python
2. Run: pip install groq python-dotenv
3. Create a .env file and add: GROQ_API_KEY=your-key
4. Run: python chat.py